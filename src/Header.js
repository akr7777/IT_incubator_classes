
const Header = () => {
    return (
        <div>
            <a href="#">Main Page</a>
            <a href="#">About</a>
            <a href="#">Contact</a>
        </div>
    );
}

export default Header;